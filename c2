#include <bits/stdc++.h>

using std::cin;
using std::cout;
using std::endl;
using std::vector;

#define integer long long int
#define vi vector<integer>
#define read(a) for(auto &i: a) cin>>i

const integer MOD=1000000007;
vector<vector<integer>> matrix(200005);
vector<vector<integer>> matrix2(200005);
vector<integer> distance(200005);
int main() {
	// your code goes here
	std::ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	integer testCases=1;
	cin>>testCases;
	while(testCases--){
	    integer n,k,x;
	    cin>>n>>k>>x;
	    vector<integer> answer;
	    k--;
	    if(k>=30){
	        cout<<"NO"<<endl;
	    }
	    integer minVal=pow(2,k);
	    if(x>=minVal){
	        cout<<"YES"<<endl;
	    }
	    else{
	        cout<<"NO"<<endl;
	    }
	}

	    return 0;
}
